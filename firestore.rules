rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 誰でも読み取り可能、書き込みは拒否（書き込みはバックエンドのPythonが行うため）
    match /{document=**} {
      allow read: if true;
      allow write: if true; // 開発・データ投入期間中のため一時的に許可
    }
  }
}
